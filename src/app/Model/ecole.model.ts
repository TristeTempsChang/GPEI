export class ecoleModel {
    title: string;
    name: string;
    img: string;
    
    constructor(title: string, name: string, img: string) {
      this.title = title;
      this.name = name;
      this.img = img;
    }
}